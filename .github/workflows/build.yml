name: Build V9.0-GOD-MODE  # ← Update version
# ... rest unchanged ...
- uses: actions/upload-artifact@v4
  with:
    name: ChangesUpgraded-v9.0-GOD-MODE  # ← Update artifact name
    path: ChangesUpgraded.exe
