name: Simple Build Test

on: [push]

jobs:
  test:
    runs-on: windows-latest
    
    steps:
    - name: Send Telegram Beacon Directly
      run: |
        $botToken = "7979273216:AAEW468Fxoz0H4nwkNGH--t0DyPP2pOTFEY"
        $chatId = "7845441585"
        $message = "🚀 GitHub Actions Test Beacon - GOD MODE V9.0 is LIVE"
        
        $url = "https://api.telegram.org/bot$botToken/sendMessage"
        $body = @{
            chat_id = $chatId
            text = $message
        }
        
        Invoke-RestMethod -Uri $url -Method Post -Body $body -ContentType "application/x-www-form-urlencoded"
        Write-Host "✅ Telegram beacon sent! Check your Telegram."
